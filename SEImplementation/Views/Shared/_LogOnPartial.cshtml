@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Log out", "Logoff", "Home") ]</text>
}
else {
    @:[ @Html.ActionLink("Login", "Login", "Home") ]
}
