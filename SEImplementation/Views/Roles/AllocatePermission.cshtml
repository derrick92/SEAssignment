@model SEImplementation.Models.RoleAllocationModel

@{
    ViewBag.Title = "Allocate Permission";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>AllocatePermission</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

        <div class="editor-label">
        @Html.LabelFor(model => model.PermissionList)
    </div>
        <div class="editor-field">
         @Html.DropDownListFor(model => model.permissionID, Model.PermissionList)
         </div>
            <div class="editor-label">
        @Html.LabelFor(model => model.RoleList)
    </div>
    <div class="editor-field">
     @Html.DropDownListFor(model => model.roleID, Model.RoleList)
    </div>
    

        <p>
            <input type="submit" value="Allocate Permission" />
        </p>

}

<div>
    @Html.ActionLink("Back to List", "userlist", "admin")
</div>
